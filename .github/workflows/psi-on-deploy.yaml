name: PSI on Deploy

on:
  push:
    branches: [main]

jobs:
  psi:
    runs-on: ubuntu-latest
    steps:
      - uses: usman-khalid/aem-psi-action@v1
        with:
          org: usman-khalid
          site: da-playground
          admin-api-key: ${{ secrets.AEM_ADMIN_API_KEY }}
